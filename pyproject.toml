[project]
name = "setup"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "ezpy-logs>=0.1.2",
    "pydantic>=2.10.6",
    "python-fire>=0.1.0",
    "pytest-ordering>=0.6",
]
